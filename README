Version: PbCdlComm 1.3.8

Run "make" to compile the binary to ../bin
Run "make install" to install in /usr/local/bin

A sample configuration file can be found in ../conf

The user manual is available at 
http://engineering.arm.gov/~choudhury/PbCdlComm/
